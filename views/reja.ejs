<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <title>Document</title>
</head>

<body style="background: #b8bbd1;">
    <div class="container">
        <h1 class="display-4 text-center py-1">REJA VA MAQSADLAR</h1>

        <div class="jumborton p-3 shadow-sm" style="width: 60%; margin: auto; margin-bottom: 30px;">
            <form id="create-form" class="create-form" action="/create-item" method="POST" >
                <div class="d-flex align-items-center">
                    <input id="create-field" name="reja" autofocus autocomplete="off" class="form-control mr-3"
                        style="flex: 1; margin-right: 10px;" type="text" />
                    <button class="btn btn-primary" style="border-radius: 20px;">
                        Yangi reja Qo'shish
                    </button>
                </div>
            </form>
        </div>

        <ol id="list-item" class="list-item list-group pb-5" style="width: 60%; margin: auto;">
            <% items.map(function(item) { %> 
                <li
                class="list-group-item list-group-item-info d-flex align-items-center justify-content-between">
                <span class="item-text">
                    <%= item.reja %>
                </span>
                <div>
                    <button
                    data-id="<%= item._id %>"
                    class="edit-me btn btn-secondary btn-sm mr-1"
                    >
                    Edit
                    </button>
                    <button data-id="<%= item._id %>" class="delete-me btn btn-danger btn-sm">
                    delete
                    </button>
                </div>
                </li>
            <% }) %>
            
        </ol>
        <button id="clean-all" class="btn btn-danger" style="margin-top:-50px; margin-left:260px; width: 150px;">
            Delete all
        </button>
    </div>
    <script src="/browser.js"></script>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>